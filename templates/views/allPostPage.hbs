<!DOCTYPE html>
<html lang="en">
<head>
    {{>baseHead}}
    <title>Home</title>
</head>
<body>
    <div class="main-container" style="background-color: rgb(184, 224, 211);">
        {{>header}}

        {{!-- loop all post --}}
        {{#each allPost}}
            <div style="background-color: rgb(232, 184, 213);">
                <div>
                    <h4><a href="/post/{{ this.id }}">{{this.name}}</a></h4>
                    <div>
                        <a href="/post/update/{{ this.id }}">Edit</a>
                        <a href="/post/delete/{{ this.id }}">Delete</a>
                    </div>

                </div>
                <p>
                    <span>{{this.date}} | </span>
                    <span>{{this.topic}}</span>
                </p>
            </div>
        {{/each}}
    </div>
    {{>footer}}
</body>
</html>